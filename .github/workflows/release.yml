name: Release

on:
  push:
    tags:
      - 'v*.*.*'
jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: build
        uses: ./github/workflows/build.yml

      # does not work with unsigned plugins!
      # - name: Lint plugin
      #   run: |
      #     git clone https://github.com/grafana/plugin-validator
      #     BIN_PATH=`pwd`
      #     pushd ./plugin-validator/pkg/cmd/plugincheck
      #     go build -o $BIN_PATH/plugincheck
      #     popd
      #     ./plugincheck ${{ steps.build.metadata_outputs_archive }}

      - name: Create release
        id: create_release
        uses: ncipollo/release-action@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag: ${{ github.ref }}
          name: Release ${{ github.ref }}
          body: ${{ steps.build.changelog_outputs_path }}
          draft: true
          artifacts: ./${{ steps.build.metadata_outputs_archive }}
          artifactContentType: application/zip

      - name: Add checksum to release
        id: upload-checksum-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.build.create_release.outputs.upload_url }}
          asset_path: ./${{ steps.build.metadata_outputs_archive-checksum }}
          asset_name: ${{ steps.build.metadata_outputs_archive-checksum }}
          asset_content_type: text/plain
